<resources>
    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@android:color/white</item>
        <item name="queryHint">Search wine</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
        <item name="android:iconifiedByDefault">false</item>
        <item name="searchHintIcon">@drawable/ic_search_hint</item> <!-- Optional: Custom search icon -->
        <item name="searchIcon">@drawable/ic_search</item> <!-- Optional: Custom search icon -->
    </style>
</resources>